# 2026-08-07 work log

- Replaced the earlier mismatched boundary diagnostic as the primary MOUF
  inference exercise. The new fixed-design bootstrap uses one deterministic
  systematic sample of 25,000 out of 524,610 exact five-minute pairs for both
  the observed and simulated likelihood ratios, simulates conditional on the
  observed lagged states under OUF, and refits the same OUF and joint
  four-parameter MOUF estimators in every replication.
- Completed 500 null bootstrap replications with four optimizer starts and four
  parallel workers. The observed likelihood ratio is 12,722.80446; the largest
  null likelihood ratio is 0.634951; no simulated statistic reaches the observed
  statistic. The finite-replication-corrected p-value is 1/501 = 0.001996.
  Alternative convergence is 98.2%, and 99.4% of null fits put the reset rate at
  its zero boundary. Inference is explicitly conditional on the prespecified
  fixed design and maintained OU null.
- Prespecified the article-facing hierarchy: OUF/MOUF are core structural
  models; heteroskedastic threshold OU is the main empirical extension;
  single/two-scale empirical-marginal copulas are dependence diagnostics;
  random walk and nested OU are forecast benchmarks; CIR/MCIR and affine
  transformations are appendix feasibility checks.
- Added a two-scale latent-Gaussian dependence model with
  `rho(h) = w exp(-kappa_fast h) + (1-w) exp(-kappa_slow h)` and its
  independence-reset mixture. The two latent OU factors form a Markov vector,
  but their observed scalar sum is generally not one-dimensional Markov; the
  model is therefore not claimed as a new instance of the article's scalar
  Markov proposition.
- Fitted SingleGaussian, SingleMixed, TwoScaleGaussian, and TwoScaleMixed on the
  same deterministic samples at six horizons. All optimizers converged with
  repeated best solutions. Full-sample equal-horizon mean log scores are
  1.42051, 1.46991, 1.52663, and 1.59503. Dependence RMSE values are 0.07022,
  0.07018, 0.00377, and 0.03223. The slow factor, not the reset mixture, is what
  repairs the long-horizon correlation curve.
- Re-estimated the empirical marginal and all copula parameters using only
  2021--2023, froze them, and evaluated 2024 and 2025. TwoScaleMixed has the
  highest equal-horizon copula log score in validation (1.66640) and test
  (1.57116), exceeding SingleMixed by 0.06780 and 0.13520. Its advantage is at
  30--360 minutes; it underperforms SingleMixed at 5 and 10 minutes in 2025.
- Updated `empirical_results_article.tex` with the mathematical definition and
  scope of the two-scale extension, the matched-bootstrap formula and results,
  model hierarchy, full-sample fit/RMSE comparison, frozen-parameter out-of-
  sample scores, revised conclusions, audit checklist, and artifact provenance.
- Added unit tests for two-scale nesting/correlation and same-design bootstrap
  behavior. All 17 tests pass. A TeX engine remains unavailable, so the document
  is verified by the static structure/reference test rather than PDF compilation.
- Rebuilt and reinstalled `meanrev-stablecoin==0.1.0` with `--no-deps` only in
  the task-specific `D:/Anaconda/envs/meanreversion-stablecoin` environment.
  Imported the new two-scale and matched-bootstrap entry points successfully;
  no other environment or dependency was modified.
