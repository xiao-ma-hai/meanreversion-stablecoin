# 2026-08-18 工作日志

## 反馈评估

- 完整阅读第二轮与第三轮反馈；确认第三轮覆盖第二轮 P0，并新增三币共同样本与频率半群设计。
- 接受连续 MOU 点密度在并列价格下无上界的诊断。
- 修正反馈中“右连续乘法权重自动具有普通强度积分表示”的条件遗漏：普通积分形式还需要累积风险局部绝对连续。

## 数据与观测模型

- 审计 USDT、USDC、DAI 的 1m/5m 六个原始 CSV。
- 建立 229,484 个三币共同、精确、不插值的五分钟转移对。
- 输出年度 tick、并列率、覆盖率、缺口和连续段审计。
- 实现 nearest-tick log bin、稳定 interval probability、OU/MOU interval likelihood 和 grid-filter 原型。
- 增加连续似然退化、区间有界、量化路径和三币统一观测测试。

## 估计与推断

- 分离 2021--2025 全样本描述估计和 2021--2023 冻结训练估计。
- 输出 2024 validation 和 2025 post-selection 的 UTC 日聚类 score difference 区间。
- 完成共同样本三币点估计和非参数漂移曲线；正式同步参数区间保持为未通过门槛。
- 完成 1m--5m 参数与 transfer-score 预检；三币均未通过半群一致性。

## 理论与论文

- 分离 drift-reverting kernel 与 initial-law mean convergence。
- strong persistence 加入收缩条件；Copula criterion 固定 $R$；generator criterion 固定时点。
- mixed local drift 加入存在条件；acceleration 限定为 conditional-mean decay。
- 新增 scalar weight necessity、centered covariance 和谱隙命题。
- 论文主实证改为三币、区间似然和频率失败结果；旧连续 LR/bootstrap 从正文撤回。
- 新增 `scripts/28_second_third_round_cycle.py`，可一键重建本轮审计、估计、图表和 Overleaf 包。

## 验收

- 62 项自动测试全部通过。
- 理论静态审计通过：无重复标签、未定义引用、缺失 citation 或 TeX 环境错误。
- 投稿包静态审计通过：14 个 TeX 文件、204 个标签、122 个交叉引用、32 个 citation、38 个 BibTeX 条目；清理未被正文引用的旧表图后，ZIP 的 19 个文件与展开目录一致。
- 当前上传包为 `submission/copula_mean_reversion_2026-08-18_overleaf.zip`，SHA-256 为 `BD549CCD169DD289F805F865EDD2FBF6D472EC9BC7D0792C5E4FFDF6E7BBBF58`。
- 本机没有 XeLaTeX；上一轮 PDF 已移入 `archive/legacy_pdfs/`，避免与当前未复编版本混淆。
- 另建约 1.35 MB replication ZIP，包含 167 个代码、配置、测试、输出和论文文件；六个大体积原始 CSV 不重复打包，但文件名、字节数和 SHA-256 均写入包内 manifest。最终归档哈希在交付记录中单独报告，以避免归档文件包含自身哈希造成循环变化。
