# 2026-08-06 work log

- Located and read `meanreversion_stablecoin_codex_methodology.tex` and
  `stablecoin_mean_reversion_empirical_design.tex` in full.
- Located the source at `../data/Kraken_OHLCVT/master_q4/USDTUSD_5.csv`.
- Independently verified source SHA-256:
  `417ea3aa71f86c4476c30568f251f3f873ae8b0d44c5f480f5ad2f35fc6acc22`.
- Confirmed the project directory is the only writable research directory.
- A named environment under `D:/Anaconda/envs` was rejected because it would
  violate the read-only boundary. Adopted a project-local Conda prefix
  `.conda-env` and recorded this implementation deviation.
- The local Conda root clone copied metadata and libraries but omitted
  `python.exe`, so it was rejected as unusable. Switched to a project-local
  `.venv` created by Python 3.11 with `--system-site-packages`; this keeps all
  writes inside the authorized project and uses the verified Anaconda stack.
- Copied the source CSV to `data/raw/USDTUSD_5.csv`, preserved byte size, and
  set the target read-only flag before any analysis.
- User subsequently authorized creating one new named environment under
  `D:/Anaconda/envs`, while explicitly protecting all existing environments.
- Classic and libmamba solvers were stopped after prolonged index stalls before
  any transaction. A read-only offline copy from `jsf` completed the new
  `meanreversion-stablecoin` target; PyYAML, pytest, and arch were installed only
  into that new target. Verified Python 3.11.5 and all required imports.
- Began reproducible project scaffold, configuration, tests, models, empirical
  scripts, outputs, and independent methodology assessment.
- Completed the raw audit exactly: 797,178 rows, 750,220 exact 300-second
  pairs, 46,957 longer gaps, 86.518594% grid coverage, no duplicates, missing
  fields, grid violations, OHLC violations, or negative counts. Raw target is
  read-only and its SHA-256 matches the methodology value.
- Prepared full, baseline (2021-2025), extended (2020-2025), and complete-bin
  15/30/60-minute datasets without interpolation. The baseline contains
  524,610 strict five-minute pairs.
- Completed nonparametric drift with blocked cross-validation and 1,000 UTC-day
  bootstrap repetitions; OU/threshold OU, CIR/depeg-pressure CIR, affine
  transformed models, MOU/MCIR, semiparametric multi-horizon copula, boundary
  LR bootstrap, rolling forecasts, event recovery, and first-passage analyses.
- Marked price CIR and affine transformed CIR as weakly identified because the
  high-noncentrality SciPy exact density failed numerically for all evaluated
  price transitions and required a conditional-moment Gaussian fallback. The
  depeg-pressure CIR did not require this fallback.
- Added 2020-2025, 15/30/60-minute complete-bin, full-sample anomaly-policy,
  and 1/3/7 UTC-day moving-block robustness outputs.
- Final one-click run executes tests internally. Final status:
  `pytest_status=PASS`, `pipeline_status=PASS`, `completion_status=PASS`;
  14 tests passed, with 46 CSV tables, 11 PNG figures, and 11 PDF figures.
- Installed `meanrev-stablecoin==0.1.0` only into the newly authorized
  environment and exported `environment.from-history.yml` and
  `environment.lock.yml`. No existing environment was modified or deleted.
- `pip check` reports inherited conflicts only among packages outside this
  project's dependency set (pdfminer/selenium/trio-typing/Jupyter-related).
  These were not changed because the empirical package does not import them;
  the complete test and pipeline suite passes, and the output is retained in
  `output/logs/environment_audit.json` for transparency.
- Removed the task-created failed local `.conda-env` and `.venv` attempts after
  verifying both resolved inside the project. They totalled about 86 MB, were
  ignored temporary copies, and are superseded by the authorized named
  environment; no data, output, or pre-existing environment was removed.
- Read `meanreversion_article_main.tex` in full and audited every empirically
  relevant theoretical construction against the implementation. Parametric
  MOU/MCIR match the article's constant-intensity mixed-copula kernel; affine
  OU/CIR match restricted linear-transformation cases. The empirical-marginal
  semiparametric copula does not automatically meet the article's marginal sign
  restriction and attains only 42.42% finite-horizon sign consistency around its
  empirical marginal mean, so it is labelled a dependence diagnostic rather
  than a verified instance of the mean-reversion proposition.
- Added `empirical_results_article.tex`, a standalone, article-style empirical
  results document with exact formulas, theory-alignment table, source-file
  provenance, likelihood and forecasting results, limitations, and an audit
  checklist. Added a static TeX structure/reference test; the full suite now has
  15 passing tests. A TeX engine is not installed on this machine, so validation
  covers balanced braces/environments, unique labels, and existence of every
  referenced figure and output file rather than PDF compilation.
