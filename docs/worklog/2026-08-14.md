# 2026-08-14 — full theory audit and corrected framework

## Scope

- Audited `../meanreversion_article_main.tex`, lines 154–674, against the read-only Chinese thesis sources `chap1.tex`, `chap2.tex`, `chap3.tex`, and `deno.tex` extracted from `基于Copula方法的马尔可夫鞅过程及动态期权定价研究.zip`.
- Kept translation fidelity and mathematical validity as separate questions.
- Checked all 52 labels in the English theory range, all named definitions/assumptions/propositions/examples, the unlabelled special-function claims, and all 25 empirical registry objects.
- Did not modify the English source or the Chinese source tree.

## Deliverables

- `theory_correction_comparison.tex`: Chinese correction-comparison report with the four requested verdict classes, complete label inventory, source-line mapping, symbol audit, proof diagnostics, model-by-model derivations, and the 25-model empirical embedding table.
- `meanreversion_article_theory_revised.tex`: standalone English corrected theory with definitions, assumptions, theorems, proofs, model conditions, reset-mixture construction, bond PDE, and empirical identities.
- `scripts/16_verify_theory_revision.py`: dependency-free static verifier for original-label coverage, model coverage, duplicate labels, undefined references, environment balance, and document termination.

## Principal mathematical decisions

- Replaced the backward-drift definition by a forward transition-kernel drift.
- Specialized the English Copula foundation to positive-time absolutely continuous densities, with row/column normalization and density Chapman--Kolmogorov consistency; ordinary state-transition kernels remain only where a physical Markov law is needed.
- Made the exact finite-horizon quantile-kernel identity primary; generator PDE sufficiency now requires extended-domain and class-(D)/uniform-integrability conditions.
- Separated target mean `m`, base diffusion level `\bar r`, Gamma scale `s`, negative coefficient `\gamma`, and positive speed `d`.
- Downgraded the two Gamma equations from sufficient conditions to necessary zero-point diagnostics plus a required global sign test.
- Proved the standard quadratic CIR transform is only finite-horizon monotone and retained the exact window constraint.
- Retained OU/CIR affine and exponential transforms with support and moment conditions.
- Classified the OU Kummer candidate as a PDE solution but a global noninjective transformation; retained the CIR Kummer family only branch by branch.
- Deleted the false CIR reduction to `u_tau=z u_zz` and supplied the correct equation with the first-derivative term.
- Reproved the mixed-copula model directly as a stationary reset kernel and isolated its exact structural advantage: conditional-mean persistence is multiplied by the no-reset survival probability.
- Separated one-dimensional structural, conditional/finite-horizon, latent-state, benchmark, and currently non-embeddable empirical identities.

## Verification

- `python scripts/16_verify_theory_revision.py`: passed; all 52 original theory labels and all 25 empirical model names are covered; no duplicate labels, undefined internal references, unbalanced environments, or content after `\end{document}`.
- `python scripts/15_verify_theory_derivations.py`: passed all symbolic zero-residual and counterexample checks; refreshed `output/tables/theory_derivation_audit.csv` and `output/models/theory_symbolic_checks.json`.
- `python -m pytest -q`: 41 passed.
- Both TeX files were compiled successfully with the XeTeX-compatible Tectonic engine, rerun to stabilize references, and rendered page by page for visual inspection.  Final logs contain no LaTeX errors, undefined references, double-superscript failures, or overfull boxes.

## XeLaTeX compatibility and density-only Copula revision

- Added explicit XeLaTeX editor directives and the missing `mathrsfs` dependency; repaired a hidden U+000C form-feed corruption, a lost `\frac`, lost `\qquad` commands, a missing `\label`, and invalid `\chi'^2` double superscripts.
- Recast the English foundation directly in terms of a jointly Borel family `c_{s,t}` for `s<t`; separated the diagonal identity `C_{t,t}(u,v)=min(u,v)`, supplied consistent finite-dimensional densities, and stated that the construction alone does not imply continuous paths.
- Made the filtration explicit in the exact martingale and strong-reversion criteria, and completed the observable-density result with normalization, marginal propagation, and physical Chapman--Kolmogorov identities.
- Closed the Gaussian, diffusion, and reset constructions under the density assumptions.  In particular, the reset-Copula formula now requires stationary start from the common invariant law and proves `c_mix=a c+(1-a)` satisfies density Chapman--Kolmogorov.
- Corrected `FullyTimeVaryingMarginalCopula` from latent class `L` to conditional class `C`: with deterministic positive scale it is itself a one-dimensional time-inhomogeneous Markov process; the additional restrictions concern reversion to a fixed target, not Markovity.
- Added controlled line breaks and narrower table spacing.  Visual inspection covered all 17 pages of each final PDF; no clipping, overlap, or margin overflow remains.
- Final checks: theory inventory verifier passed (52/52 original labels, 25/25 empirical models), symbolic theory audit passed all nine checks, and `pytest -q` reported 41 passed.
- Removed the temporary local Tectonic binary/archive and rendered-page QA images after copying the checked PDFs to `output/pdf/`; compile logs remain under `output/tex_compile_check/after/`.

## Detailed proofs appendix

- Added Appendix A, Detailed proofs, to meanreversion_article_theory_revised.tex.
- Proved the short-time moving-kernel limit under both bounded uniform-norm and unbounded weighted-norm conditions; explicitly noted that pointwise time differentiability alone is insufficient because the transition measure changes with h.
- Expanded the quantile-generator drift identity entirely in transition-density integrals, including the push-forward conditional kernel, exact two-term difference-quotient decomposition, and almost-everywhere qualification.
- Added detailed proofs for the finite-horizon conditional expectation criteria, observable transition-density normalization/marginal propagation/Chapman--Kolmogorov identities, the generator martingale equivalence, the rescaling PDE, strict diffusion transforms, and stationary reset mixtures.
- Strengthened the main generator martingale theorem so that class-(D) explicitly refers to the uniformly integrable family indexed by all bounded stopping times.
- Actual Tectonic/XeTeX-compatible compilation succeeded after reference stabilization: 22 A4 pages, no LaTeX errors, undefined references, overfull boxes, or multiple-label errors. Appendix pages 18--22 and the modified main-text page 3 were rendered and visually checked.
- Final regression checks: theory inventory verifier passed with 162 labels and no undefined or duplicate labels; all nine symbolic derivation checks passed; pytest -q reported 41 passed.
