# 2026-08-17 工作日志

## 理论

- 对照 major-revision 计划完成 P0 理论量词检查。
- 修正 strong-to-local 命题的 Markov、核版本、共同满测集和 a.e. 结论。
- 修正扩散变换逆命题：先得到 dt×Law(R_t)-a.e. PDE，再在连续性和满支撑下升级。
- 把生成元符号命题的极限交换改写为移动核引理的显式充分条件。
- 把 mixed reset 的精确条件均值公式与严格加速推论分离。
- 修正 CIR reduction 标题，并解释非正反向时间坐标。

## 实证

- 新增不变边缘参数化 theta, tau, kappa, lambda。
- 新增 MOU-Peg、MOU-IFM、MOU-UQML 三种估计口径。
- 新增潜在 OU + 观测噪声 Kalman 精确似然基准。
- 新增全样本、训练、验证、测试的不变边缘审计和硬门槛。
- 新增三组 MOU-UQML 二维识别切片。
- 修正 Copula pseudo-observations 为 mid-rank；增加随机并列值 PIT 接口。
- 新增保持真实连续片段长度的 OU-null 路径 bootstrap pilot。

## 验证

- 修改前基线：41 tests passed。
- 最终测试：47 tests passed。
- 真实数据核心估计已写入 output/tables、output/models 和 output/figures。

## 尚未越过的阶段门槛

- 路径 bootstrap 正式门槛 B≥1999；当前每种初值 B=99。
- Gaussian MOU-IFM/Peg 虽修复均值与尺度，仍未通过高峰厚尾边缘门槛。
- 严格 profile、分块 Godambe、价格代理/频率敏感性、外部样本仍待下一轮。

## 工作区整理

- 当前可上传包统一为 `submission/copula_mean_reversion_major_revision_overleaf.zip`，入口为 `main.tex`。
- 可直接检查的展开版统一为 `submission/overleaf/`，编译参考稿位于 `submission/pdf/`。
- 理论单一来源移至 `manuscript/source/`，理论审计报告移至 `manuscript/audits/`。
- 旧 `overleaf_article`、旧压缩包和早期独立实证稿完整移入 `archive/`，未删除历史文件。
- 生成脚本、静态检查和测试中的路径已同步更新。
